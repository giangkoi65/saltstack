server {
    listen {{ salt['pillar.get']('nginx:port', 80) }};
    server_name localhost;

    root /var/www/mysite;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
